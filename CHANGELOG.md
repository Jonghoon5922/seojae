# 바뀐 것

## 0.2.0

### 설치 파일 하나로 Claude Desktop까지 붙는다

전에는 `.mcpb` 를 따로 받아 더블클릭해야 했고, 그 전에 uv를 먼저 깔아야 했다.
이제 설치 화면의 체크박스("Claude Desktop에 서재를 등록합니다")를 켜두면
설정 파일에 항목이 자동으로 들어간다. **uv도 PyPI도 필요 없다.**

- 앱과 MCP가 **같은 설정을 보고 같은 서재를 연다.** 설정 화면에서 서재를 옮기면
  Claude가 보는 서재도 따라 옮겨진다
- 제거하면 등록도 같이 지워진다. 서재 폴더(내 문서)는 건드리지 않는다
- 다른 MCP 서버 설정은 손대지 않고, 고치기 전에 `.bak` 백업을 남긴다
- 나중에 하거나 되돌리려면: `seojae-mcp.exe register` / `unregister`

실행 파일이 둘이 됐다. 창 모드 실행 파일에는 표준 입출력이 없어서 MCP가 오갈
통로가 없기 때문이다 (파이썬이 `python.exe` / `pythonw.exe` 로 나뉜 것과 같은 이유).

    서재.exe        창 모드   아이콘 더블클릭 → 앱 창
    seojae-mcp.exe  콘솔 모드 Claude Desktop이 stdio로 대화

### 대출 기록 — 누가 언제 왜 꺼내 갔는지

기록 탭에 표가 하나 늘었다. 위쪽 "옮긴 기록"이 내가 한 일이라면, 이쪽은
**Claude가 한 일**이다.

| 언제 | 누가 | 무엇을 | 왜 | 건 |
|---|---|---|---|---|
| 09-09 10:33 | Claude Desktop 0.14.2 | search | 블록체인 합의 알고리즘 · 헛걸음 | 3 |
| 09-09 10:33 | Claude Desktop 0.14.2 | search | 예외 처리 규약 | 3 |

"왜"가 검색어인 이유는, 이 앱이 LLM을 부르지 않아 의도를 물어볼 길이 없어서다.
어떤 말로 찾았는지가 의도에 가장 가깝다.

쌓이면 서재를 고치는 재료가 된다.

- **헛걸음 목록** — 서재에 없는 주제, 또는 말이 어긋난 것. 뭘 채울지 알려준다
- **안 꺼내진 문서** — 아무도 안 찾은 책. 정리 대상

앱에서 한 검색도 같은 표에 `앱` 으로 남는다. 기록은 서재 안에만 있고
아무 데도 보내지 않는다.

CLI: `seojae readings <폴더>` (`--missed`, `--unread`)

### 고친 것

- **cp949 오류로 죽던 문제.** `seojae --help` 를 파이프로 넘기면 터졌다.
  설치 프로그램이 명령을 숨겨서 실행하는 것이 곧 파이프라 등록도 실패했을 것이다
- **UTF-8 BOM이 붙은 설정 파일을 "깨졌다"고 거부하던 문제.** 메모장이나
  PowerShell로 고치면 붙는다
- 문서에 남아 있던 `_inbox` → `_미분류`

### 안쪽

- 버전이 한 곳(`src/seojae/__init__.py`)에만 있다. 어긋나면 테스트가 잡는다
- 테스트 231건

---

## 0.1.0

첫 공개.

- 폴더 하나가 책장 하나. `README.md` 의 설명이 Claude가 책장을 고르는 근거가 된다
- 형식 11종, 확장자 60개 (한글 hwpx · PDF · Word · Excel · PowerPoint ·
  HTML · 소스코드 36종 · 이미지)
- kiwipiepy 형태소 분석 + SQLite FTS5 BM25. 임베딩 모델을 내려받지 않는다
- 모든 검색 결과에 출처(파일 경로 + 헤딩·페이지·시트·줄번호)가 붙는다
- 재검색 힌트 — 이 서재가 쓰지 않는 말을 알려주고 실제로 쓰는 용어를 준다
- `_미분류` 에 던져두면 Claude가 읽고 알맞은 책장으로 옮긴다. 모든 이동은 되돌릴 수 있다
- 앱 화면 5개 (검색 · 책장 · 미분류 · 기록 · 설정), 드래그앤드롭
- MCP 도구 9개
